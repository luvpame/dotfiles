[post-start]
herdr-workspace = '''
if [ "${HERDR_ENV:-}" = "1" ] && [ -n "${HERDR_WORKSPACE_ID:-}" ]; then
    herdr worktree open --workspace "$HERDR_WORKSPACE_ID" --path {{ worktree_path }} --focus
fi
'''
