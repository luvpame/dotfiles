[alias]
  s   = switch
  st  = stash -u
  stp = stash pop
  sta = stash apply
  a   = add
  c   = commit
  p   = pull
  ps  = push
  d   = "!echo 'git clean -fd && git restore .' && git clean -fd && git restore ."
  f   = fetch
[color]
  ui = true
[pull]
  ff = only
[init]
  defaultBranch = main
[gpg]
  format = ssh
[gpg "ssh"]
  program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
[commit]
  gpgsign = true
[push]
  autoSetupRemote = true
[core]
  editor = nvim
  pager  = delta
  diff   = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    syntax-theme = Catppuccin Latte
    light = true
    navigate = true  # use n and N to move between diff section
    side-by-side = false
    line-numbers = true
[merge]
    conflictStyle = zdiff3
[include]
  path = ~/.config/git/config.local
