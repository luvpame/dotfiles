onboarding = false
[keys]
prefix = "ctrl+j"

new_tab = "prefix+c"
cycle_pane_next = "prefix+tab"
split_vertical = "prefix+|"
split_horizontal = "prefix+minus"
resize_mode = "prefix+r"

focus_pane_left = "prefix+h"
focus_pane_down = "prefix+j"
focus_pane_up = "prefix+k"
focus_pane_right = "prefix+l"

navigate_pane_left = "h"
navigate_pane_down = "j"
navigate_pane_up = "k"
navigate_pane_right = "l"

[[keys.command]]
key = "prefix+d"
type = "shell"
command = "pane_ids=$(herdr pane list | jq -r --arg pane_id \"$HERDR_ACTIVE_PANE_ID\" '.result.panes as $panes | ($panes[] | select(.pane_id == $pane_id).tab_id) as $tab_id | $panes[] | select(.tab_id == $tab_id and (.label == \"diff view\" or (.terminal_title // \"\" | contains(\"hunk diff\")))) | .pane_id'); if [ -n \"$pane_ids\" ]; then for pane_id in $pane_ids; do herdr pane close \"$pane_id\"; done; else pane_id=$(herdr pane split \"$HERDR_ACTIVE_PANE_ID\" --direction right --no-focus --ratio 0.66 | jq -r '.result.pane.pane_id') && herdr pane rename \"$pane_id\" \"diff view\" && herdr pane run \"$pane_id\" 'hunk diff'; fi"

[[keys.command]]
key = "prefix+g"
type = "pane"
description = "open ziggity"
command = "ziggity"

[[keys.command]]
key = "prefix+o"
type = "popup"
description = "open ghq workspace"
command = "fish -c 'ghq_cd_fzf --workspace; set workspace_status $status; if test $workspace_status -ne 0; read -P \"Press Enter to close\"; end; exit $workspace_status'"
width = "80%"
height = "80%"

[[keys.command]]
key = "prefix+shift+g"
type = "popup"
description = "review requested PR"
command = "fish -c 'review-pr; set review_status $status; if test $review_status -ne 0; read -P \"Press Enter to close\"; end; exit $review_status'"
width = "80%"
height = "80%"

[[keys.command]]
key = "prefix+t"
type = "popup"
description = "open floating terminal"
command = "fish"
width = "80%"
height = "80%"

[theme]
name = "catppuccin-latte"
auto_switch = false

[ui.toast]
delivery = "terminal"

[ui]
show_agent_labels_on_pane_borders = true
sidebar_width = 72
sidebar_max_width = 72
confirm_close = false
hide_tab_bar_when_single_tab = true
agent_panel_sort = "priority"

[ui.sidebar.agents.rows_by_agent]
claude = [
  ["state_icon", "workspace", "tab"],
  ["agent"],
  [
    { token = "$context", fg = "#1e66f5" },
    { token = "$model", fg = "#8839ef" },
    { token = "$effort", fg = "#fe640b" },
  ],
]
codex = [
  ["state_icon", "workspace", "tab"],
  ["agent"],
  [
    { token = "$context", fg = "#1e66f5" },
    { token = "$model", fg = "#8839ef" },
    { token = "$effort", fg = "#fe640b" },
    { token = "$fast_mode", fg = "#df8e1d" },
  ],
]

[experimental]
pane_history = true
switch_ascii_input_source_in_prefix = true
